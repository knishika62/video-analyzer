# h3_video2prompt.py の LLM エンドポイント設定
# このファイルを .env にコピーして値を変更する
# 優先順位: CLI引数 (--api-base / --model) > .env / 環境変数 > スクリプト内既定値

# OpenAI API 互換のエンドポイント（/v1 を含む）
H3_API_BASE=http://192.168.11.100:8888/v1

# 画像/動画解析用のビジョンLLMのモデル名
H3_MODEL=qwen38-27b-dflash2
